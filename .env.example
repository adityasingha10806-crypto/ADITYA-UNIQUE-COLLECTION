# Admin login credentials (choose your own)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# Any long random string (used to sign login sessions)
SESSION_SECRET=replace-with-a-long-random-string

# These two are auto-filled by Vercel when you connect a KV database
# (Storage tab -> Create Database -> KV -> Connect to Project)
KV_REST_API_URL=
KV_REST_API_TOKEN=
